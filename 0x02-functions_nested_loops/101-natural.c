#include <stdio.h>
/**
 * main - main function
 * Return: void
 */
int main(void)
{
	int n;
	int s;

	n = 0;
	while (n < 1024)
	{
		if (n % 3 == 0 || n % 5 == 0)
		{
			s += i;
		}
		n++
	}
	printf("%d\n", s);
	return (0);
}
