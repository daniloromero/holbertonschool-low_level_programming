#include "holberton.h"
#include <stdlib.h>

/**
 * arstostr - concatenates all the arguments of your program
 * @ac: input
 * @av: input
 * Return:0  free memory
 */
char *arstostr(int ac, char **av)
{
	int index;
	char str;

	if (ac == 0 || ac == NULL)
		return (NULL);
	return (str);
}
